{
  "name": "linkedin-ai-comments",
  "version": "1.2.1",
  "description": "A Chrome extension that uses AI to generate comments on LinkedIn",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && webpack --config webpack.config.js",
    "dev": "webpack --watch --config webpack.config.js --mode=development",
    "format": "prettier --write \"src/**/*.{ts,css,html,json}\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "copy-webpack-plugin": "^12.0.2",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.10",
    "ts-loader": "^9.5.1",
    "typescript": "^5.7.3",
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4"
  }
}
